# Errors Log — Financial Planning Tool Studio
# Purpose: every caught defect. Every finplan-* skill reads this BEFORE starting work.
# Schema (adapted for a code product): # | Date | Pattern | Location | Defect | Correct behavior | Source of truth | Owner + status
# Append-only. Do not delete rows. A defect that recurs is a protocol failure — escalate to Critical.

---

## How to use this file

1. **At session start** — every finplan-* skill reads this file. The "Open Patterns" section at the bottom summarizes what to watch for.
2. **During work** — when a defect matches a past pattern, cite the row number. The Auditor treats every row as a live regression test: scan the change against all rows; a recurrence is Critical, not minor.
3. **When a defect is fixed** — change its status to `FIXED YYYY-MM-DD` and add the QA test that now guards it. Never delete the row.
4. **At session end** — append new defects found this session.

Patterns are numbered 1–5 to match `lessons.md`.

---

## Error Log (append-only)

| # | Date | Pattern | Location | Defect | Correct behavior | Source of truth | Owner + status |
|---|------|---------|----------|--------|------------------|-----------------|----------------|
| 1 | 2026-06-06 | 2 (false-rigor labeling) | `Step4_InvestmentPath.tsx` (the "Probability Cone") | Labeled a probability cone / uncertainty envelope but renders three deterministic lines (base, base−2%, base+2%). Implies a stochastic simulation that does not exist. A nervous first-timer reads it as a real probability. | Relabel as a scenario range ("optimistic / expected / cautious") until a real Monte Carlo with a stated success probability ships. | Audit 2026-06-06; canonical §8 | Relabeled 2026-06-06: "Scenario Range" + "deterministic … not a probability forecast" + Expected/Cautious labels (glossary-compliant). False-rigor resolved. A real "probability" cone returns only when finplan-quant ships Monte Carlo (Phase 2). **FIXED (relabel) 2026-06-06.** — UPGRADE IN PROGRESS 2026-06-07: real seeded Monte Carlo engine shipped in `financialPlan.ts` (`runMonteCarlo`, canonical §10) and validated (27/27 tests; success rate cross-checked vs WolframAlpha ~73%; median<mean skew confirmed). `results` now exposes `successProbability`, `mcCone` (p10/p50/p90), `medianPortfolio`. **REAL CONE SHIPPED 2026-06-07:** Step 4 reads `res.mcCone` and renders a 10th–90th percentile cone + median line + a headline "X% of 1,000 simulations fund all N years"; Step 5 carries the success metric; the deterministic ±2% cone was deleted from the store; all labels glossary-honest ("probability," "percentile," "estimate, not a guarantee"). DOM-verified + test-locked (27/27); Recharts SVG visual paint pending a real-browser pass (headless throttles ResizeObserver). Audit: monte-carlo_v1.md (8.5/10). **FIXED (real probability cone) 2026-06-07.** |
| 2 | 2026-06-06 | 1 (multi-engine divergence) | `financialPlan.ts:154` vs `:240` vs `Step4_InvestmentPath.tsx` | Three projection engines (annual-add; intra-year monthly; component-local monthly) produce different answers for identical inputs. Step 5 and Step 4 can show different projected totals for the same plan. | Exactly one engine in `financialPlan.ts`; every screen reads from it. No component recomputes a projection. | canonical §0 | Model RATIFIED 2026-06-06 (finplan-cfp, canonical §3 + §5: effective-monthly simulation; reference projectedPortfolio = $1,574,534.16). Single engine `projectAccumulation` shipped in financialPlan.ts; Step 4 reads `res.coneSeries`; netWorthData reuses it. Guarded by financialPlan.test.ts (row-2 cross-screen test + projectedPortfolio = $1,574,534.16). Build + 8/8 tests green. **FIXED 2026-06-06.** |
| 3 | 2026-06-06 | 5 (silent state loss) | `financialPlan.ts` (`map`, not `persistentMap`); `package.json:17` | `@nanostores/persistent` is a dependency but unused. A page refresh wipes all user input, directly contradicting the stated Housel anti-abandonment philosophy. | Persist `inputs` to localStorage via `persistentMap`. | package.json; CLAUDE.md vision | `inputs` migrated to `persistentMap('finplan:', …)`. Guarded by financialPlan.test.ts (row-3 persistence test). **FIXED 2026-06-06.** |
| 4 | 2026-06-06 | 3 (canonical drift) | components referencing `*-shiny-*`; `tailwind.config.mjs`; `global.css` | Components use `*-shiny-*` utilities that exist in neither the config nor the CSS. Tailwind v4 likely does not load the JS-config palette (no `@theme` block, no `@config`). Much styling silently no-ops; the look is carried by inline styles + `.fintech-*` classes. | Migrate the palette to an `@theme` block in `global.css`; remove all `*-shiny-*` references. | canonical §6 | Designer + Engineer. Verified empirically: BOTH vocabularies were dead — built CSS had 0 `shiny` rules AND 0 JS-config-namespace rules (`#94a3b8` rendered 0×). Fix: canonical §6 palette migrated to an `@theme` block in `global.css` (v4 way); all 149 `*-shiny-*` refs across 13 files swapped to real tokens; `tailwind.config.mjs` deleted; ignored `variant` props on `FintechCard`/`MetricCard` wired to accent top-borders (verified `variant="info"` → 2px `rgb(59,130,246)`). Guarded by `src/design-system.test.ts` (no-shiny + @theme-present + variant-wired). Build green, 13/13 tests, tokens confirmed resolving in built CSS + on live elements (Welcome/Step1/mobile). Audit: studio/audits/dead-token-layer_v1.md (8.6/10). **FIXED 2026-06-07.** |
| 5 | 2026-06-06 | 4 (accessibility regression) | global theme; `MoneyInput.tsx`, `RangeSlider.tsx` | `#475569` text computes to 2.36:1 (fails all). `#64748b` body text computes to 3.07–3.75:1 (fails AA for normal text). | Body text uses `#94a3b8` or lighter; `#64748b` large text only; `#475569` never carries text. | canonical §7 (WCAG 2.2, computed) | A11y loop. All inline `#475569`/`#64748b` text across live screens (Welcome, Step1–5, NavigationTabs) swapped to `#94a3b8` (text.secondary); 13 `text-text-muted` body usages → `text-text-secondary`; input placeholder → `#94a3b8`. Live-verified: **0** elements compute the banned colors on Step 1 / Step 2; `.text-text-secondary` computes `rgb(148,163,184)` (6.96:1). Guarded by `src/accessibility.test.ts` + jsx-a11y gate. Audit: accessibility-floor_v1.md (8.7/10). **FIXED 2026-06-07.** |
| 6 | 2026-06-06 | 4 (accessibility regression) | `MoneyInput.tsx`, `RangeSlider.tsx`, `NavigationTabs.tsx` | `<label>` rendered without `htmlFor`/`id`; zero `aria-*` across components; the tab nav is not marked up as a tablist; no focus management on tab change. | Associate every label; apply WAI-ARIA APG patterns for tabs/sliders/disclosure; manage focus and announce on navigation. | Audit 2026-06-06; WCAG 1.3.1, 4.1.2 | A11y loop. `MoneyInput`/`RangeSlider` associate labels via `useId()` (`htmlFor`/`id` + `aria-describedby`; slider adds `aria-valuetext`); verified accessible name "Monthly Take-Home Income". `NavigationTabs` → APG Tabs pattern (2 tablists, 14 tabs, `tabpanel`, `aria-selected`, roving tabindex, Arrow/Home/End + focus management) — verified live (ArrowDown/End/Home move selection + focus, panel updates). Completion dots `role="img"`. **Live audit also caught + fixed** an unlabeled-slider gap jsx-a11y missed: Step 2's 11 sliders now carry `aria-label`. jsx-a11y gate on/green. Guarded by `src/accessibility.test.ts`. Audit: accessibility-floor_v1.md. **FIXED 2026-06-07.** |
| 7 | 2026-06-06 | 5 (input foot-gun) | `financialPlan.ts:81-84` | `inputs.subscribe` force-sets `monthlyContrib` to the Step 1 investment sum whenever that sum is >0. A contribution a user hand-enters in Step 4 can be silently overwritten when they revisit Step 1. | Gate the contribution sync; never clobber a user-entered value without explicit intent. | financialPlan.ts:81-84 | Engineer + QA (Wave 2). Added `hasModifiedContrib` (mirrors `hasModifiedRetirement`): Step 4's contribution input sets it true on edit — **flag written BEFORE the value**, because the smart-default subscriber fires synchronously on `setKey` and would otherwise re-clobber the just-typed value. The sync is now gated `totalInvest > 0 && !val.hasModifiedContrib`, so it still seeds `monthlyContrib` from the Step 1 investment sum until the user takes manual control; after that, intent wins. Guarded by `financialPlan.test.ts` row-7 test (seed still works AND a hand-entered $2,500 survives a later Step 1 investment bump). Verified in Claude_Preview end-to-end: Step 1 401(k) $1,000 → contribution auto-seeded "$1,000"; hand-entered "$2,500" in Step 4; changed Step 1 401(k) to $1,500; contribution stayed "$2,500" (not re-synced) — the flag-before-value ordering holds in the live UI. **FIXED 2026-06-07.** |
| 8 | 2026-06-06 | 1 (dead/duplicate logic) | `financialPlan.ts:132-151` | Dead `r == g` branch: the `if` and `else` run identical loops; the comment promises a closed-form geometric handling that was never implemented. | Collapse to one loop, or implement and test the documented closed form. | financialPlan.ts:132-151 | Engineer + QA (Wave 2). Verified resolved by the single-engine rewrite: the dead `r == g` branch was deleted when the old if/else loops became the single `projectAccumulation` month-by-month loop, which has **no branch on r vs g**. Confirmed empirically — `grep` finds no `r == g`/`=== g` in `src/`, and `git log -S "r == g"` shows the branch added in `a967ceb` (initial launch) and removed in `72414b6` (single-engine refactor). Guarded by `financialPlan.test.ts` row-8 **continuity/monotonicity property test**: `projectedPortfolio` stays finite, strictly monotonic, and continuous as `contribIncrease` crosses `annualReturn` (a re-introduced special-case at g==r would surface as a jump or a break in monotonicity). The stale eslint `no-useless-assignment`→row-8 comment was removed in Wave 2, and the genuinely-dead `withdrawalRate` initial value it had been masking was deleted too. **FIXED 2026-06-07.** |
| 9 | 2026-06-06 | 2 (false-rigor labeling) | `financialPlan.ts:113` | All passive income (SS, pension, other) inflated at full CPI, presented without caveat. Overstates future income, understates the required portfolio; reads as conservative when it is optimistic. | Caveat the simplification now; model SS COLA and pension treatment in Phase 2. | canonical §2 | CFP + Quant + Engineer. RATIFIED (canonical §2): **SS grows with CPI (COLA, defensible — it was never the real error), pension + other are FIXED NOMINAL** (no COLA — conservative, no toggle, stated plainly in the UI). The drawdown (deterministic + MC) is now **per-year income-aware**: `withdrawal_t = max(0, inflatingNet×(1+infl)^(t−1) − flatIncome)`, so a pension's real value erodes and success rates honestly drop. `requiredPortfolio` unchanged (year-1 25× snapshot). **Tax: caveated, not modeled** (pre-tax; a full tax engine is a separate future loop). UI: Step 3 helpers + Step 5 disclaimer make all of this explicit. Guarded by `financialPlan.test.ts` (flat pension < COLA SS success, same requiredPortfolio). 30/30 tests; verified no-pension plans unchanged. **FIXED 2026-06-07.** |
| 10 | 2026-06-07 | 4 (accessibility regression) | `NavigationTabs.tsx` (AnimatePresence `mode="wait"`); every Step entrance animation; no `MotionConfig`/`useReducedMotion` | App content is gated on Framer Motion animations completing. With `requestAnimationFrame` throttled or paused (background tab, low-power, and — the user-facing case — a reduced-motion preference), entrance animations freeze at `opacity:0` and `AnimatePresence mode="wait"` never finishes the exit, so the content area is **blank and navigation stalls**. No `prefers-reduced-motion` fallback exists. Found during the row-4 preview pass (rAF measured at 0 frames/500ms in headless). | Respect `prefers-reduced-motion`: render content immediately (skip/disable entrance + exit animations) so visibility and navigation never depend on an animation frame. | WCAG 2.2 (2.3.3, robustness); canonical §6 ("respect prefers-reduced-motion") | A11y loop. `NavigationTabs` reads `useReducedMotion()` and, when reduced, uses `initial={false}` + instant exit + `duration:0` so content/navigation never depend on an animation frame; `global.css` adds a `prefers-reduced-motion` reset. Verified by temporarily forcing the reduced path: content rendered at `opacity:1` immediately and the `AnimatePresence` stall disappeared (reverted after). Guarded by `src/accessibility.test.ts`. **FIXED 2026-06-07.** |
| 11 | 2026-06-07 | 1 (dead/duplicate logic — consistency) | `financialPlan.ts` drawdown (section E) + the new Monte Carlo | The deterministic drawdown withdrew **gross** `futureAnnualNeed`, ignoring SS/pension income, while `requiredPortfolio` (§2) correctly nets it out (`netNeed`). The two halves of the model disagreed on what the portfolio funds, understating how long it lasts. Found while wiring the Monte Carlo. | Both the deterministic drawdown and the MC withdraw `netNeed` (spend − passive income), inflation-grown — consistent with §2. | financialPlan.ts §2/§4; canonical §2 | Engineer (with Quant). Fixed in both paths 2026-06-07; covered by the §10 MC tests. Step 5 net-worth chart now depletes per net need (slower, correct) — verify on screen in the Wave-1 UI pass. **FIXED 2026-06-07 (pending on-screen confirm).** |
| 12 | 2026-06-07 | 2 (false-rigor / misleading view) | `Step5_Summary.tsx` net-worth chart | Step 5 plotted only the smooth deterministic **mean** path. After the §4 realistic-return + net-need fixes, well-funded plans **rise** through retirement (e.g. $1.57M → $2.58M) — reading as "you get richer" right next to a 53% success probability. False reassurance that hides sequence-of-returns risk (the failure mode §4 itself flags). Caught by Preston in-browser. | MC extended through the drawdown (`mcNetWorthCone`). First attempt drew a full 10th–90th band, but the right-skewed distribution made p90 compound into the tens of millions, squashing the median (2nd Preston catch). Final design: plot the **median (typical) path + a downside band (10th pct → median)** on a Y-axis clamped to ~1.5× max(target, peak median); the explosive upside is reported via the success rate + tooltip, not drawn. | canonical §4, §10 | Quant + Engineer. Guarded by `financialPlan.test.ts` (full-lifecycle length + p10→0 depletion). **FIXED 2026-06-07 (chart paint pending real-browser confirm).** |
| 13 | 2026-06-07 | 1 (multi-engine divergence) + 2 (false-rigor) | `Step5_Summary.tsx` + `Step4_InvestmentPath.tsx` headline metrics | The headline "Projected Portfolio / surplus / progress / 100% of target" read the **deterministic mean** (`projectedPortfolio`), while the net-worth chart + success rate showed the **MC median** — two different "projected" numbers on one screen (e.g. mean $2.04M "surplus" vs median ~$1.6M shortfall, next to 54% success). The rosier mean drove a falsely reassuring headline. Caught by Preston in-browser ("failing your own UI/UX audit"). | One headline number: the **median** drives Projected, surplus/shortfall, progress, and additional-needed across Step 4/5 (`medianPortfolio`, `medianGap`), matching the chart + success rate. The deterministic mean is retained as the engine value, not headlined. | canonical §10.6 | Quant + Engineer. Guarded by `financialPlan.test.ts` (medianGap reconciliation + median<mean). **FIXED 2026-06-07.** |
| 14 | 2026-06-07 | 2 (trust / clarity) | `Step5_Summary.tsx` Required Portfolio card | The "Required Portfolio" headline showed a large number with no visible driver, so users read it as the tool being broken ("$1.5M + $5k/mo and still not enough?!"). The number is correct — `required = retirement spend × inflation × 25×`, and the portfolio doesn't change it — but the *spend* driver was invisible, and retirement spend is auto-seeded from the Step 1 budget (often high). Caught by Preston in-browser. | Surface the driver on the card: the monthly spend it funds + that it is spend-driven and editable ("Funds your $X/mo (today's $)… Driven by your spending — lower it in Step 2 to lower this."). | UI/UX charter; canonical §2 | UI/UX + Engineer. Verified in-app ($4.61M → "Funds your $11,000/mo…"). A scary number must always show what drives it. **FIXED 2026-06-07.** |
| 15 | 2026-06-07 | 2 (trust / clarity) | `Step5_Summary.tsx` + `Step4_InvestmentPath.tsx` headline figures | Required/Projected/Surplus were shown only in **nominal future dollars**, so "the amount you need to retire" **decreased the earlier you retire** (fewer years of inflation compounded to a nearer date) — backwards for a "what you need" figure. Verified: same lifestyle → $2.0M required retiring in 10y vs $3.1M in 25y, a pure denomination artifact (in today's $ both are identical). Caught by Preston. | Show headline figures in **today's dollars** (stable anchor = nominal ÷ inflationMult) with the nominal "≈ $X in {retYear}" beside it — both shown. Engine + charts stay nominal (chart labeled "future dollars"). Verified in-app: today's-$ Required = $2.55M whether retiring in 10y or 20y; only the nominal moves. | canonical §2 (display convention) | UI/UX + Engineer. **FIXED 2026-06-07.** |
| 16 | 2026-06-07 | 2 (false-rigor / misleading empty state) | `Step5_Summary.tsx` (4 metric cards + progress bar + success %); `Step4_InvestmentPath.tsx` (gap panel + outcome-strip badges) | Zero-target state: with no retirement spend entered, `annualRetSpend === 0 → requiredPortfolio === 0`. `progressPct` divides by `Math.max(1, requiredPortfolio)`, so Step 5 showed **"Required $0 / 100% of target reached / Surplus / 100% success,"** and Step 4's outcome strip read **"✓ on track"** for all three percentiles against a $0 target. A first-timer who simply hasn't entered a plan reads "you're done / over-funded." Surfaced in the Step 5 cold audit (Lens 1 Critical, `step5-summary_v1.md`) and named in SWEEP-PLAN Wave 2 as "the zero-target-state bug," but never had its own row until now. | Detect the unentered-plan state (`planReady = res.annualRetSpend > 0`) and render a prompt, not fake good news. Gate on `annualRetSpend`, NOT `requiredPortfolio`: the latter can legitimately be $0 when passive income fully covers spending (a real funded state, not an empty one). | `step5-summary_v1.md` (Lens 1); canonical §2 | UI/UX + Engineer + QA (Wave 2). Step 5 early-returns a prompt ("Your summary isn't ready yet — add spending in Step 1, design retirement in Step 2") with a "Start with Step 1 →" CTA (`onEditPlan` → `setActiveTab('step1')`) when `!planReady`. Step 4 gates its On-Track panel on planReady, adds a "No target set yet" panel, and neutralizes the outcome-strip color + suppresses the ✓/✗ verdict when there is no target. Guarded by `financialPlan.test.ts` row-16 trigger test (default plan → annualRetSpend 0 & requiredPortfolio 0; entering spend flips both > 0). Verified in Claude_Preview: a default (unentered) plan shows Step 5's "Your summary isn't ready yet" prompt + "Start with Step 1 →" CTA (no "$0 / 100% / Surplus"), and Step 4 shows a "No target set yet" panel with a neutral (grey #cbd5e1) outcome strip and no ✓/✗ verdict; entering rent (seeds retirement spend) flips Step 5 to real metrics (Required $150k today's-$). 33/33 tests green. **FIXED 2026-06-07.** |
| 17 | 2026-06-07 | 2 (trust / clarity) | `Welcome.tsx:70` (landing stats strip) | The privacy/trust stat rendered as **"$0 data"** (value) / "Runs in-browser" (label) — inconsistent with its number-led siblings (`4%`, `25x`, `5 Steps`): a stray `$` on a non-currency figure plus the word "data" baked into the value, so the single most important trust signal (no data leaves the browser) read as a malformed price rather than a clean stat. Same family as rows 14/15 — a trust figure that was hard to read at a glance. | Number-led value, no stray symbol, words carried by the label, matching `4%`/`25x`: `{ value: '0', label: 'Data leaves your browser' }` → reads "0 — Data leaves your browser" (zero data leaves). | UI/UX charter; Preston's catch | UI/UX (Wave 2 follow-up). `Welcome.tsx:70` → `{ value: '0', label: 'Data leaves your browser' }`. Lint 0 errors, build green. **FIXED 2026-06-07.** |
| 18 | 2026-06-08 | 4 (accessibility regression — contrast recurrence) | `Welcome.tsx:186,450`; `Step1_CurrentReality.tsx` (budget-panel labels, % subtitles, "Ramit's target", attribution) | Row-5 recurred through colors the Wave-0 scan didn't name: Welcome rendered the **privacy promise** and a hint in `#334155` (1.72:1, FAIL all — §7), and Step 1 used `rgba(255,255,255,0.45/0.30/0.20)` (4.20 / 2.64 / 1.82:1) for ~11 text nodes. The single most important trust signal (no data leaves the browser) was nearly invisible. | Body text uses `#94a3b8` (6.96:1). `#334155`/`#475569` and sub-AA white-alpha never carry text. | canonical §7 (extended Wave 3); welcome_v1.md / step1-current-reality_v1.md | A11y (Wave 3). Welcome `#334155`→`#94a3b8` (×2); Step 1 all `rgba(255,255,255,α)` text → `#94a3b8`. Live-verified: privacy text **1.72→6.96:1**, Step 1 attribution/labels/targets **→6.96:1**. Guarded by `accessibility.test.ts` rows 18–19 (`\bcolor:` scan for `#334155`/`#475569` + sub-AA white-alpha). **FIXED 2026-06-08.** |
| 19 | 2026-06-08 | 2 (false-rigor / glossary) | `Step2_RetirementDesign.tsx:299` | Section heading "**Discretionary Spending** in Retirement" uses the glossary's explicitly-banned alternative to Sethi's **Guilt-Free Spending** (glossary.md, the load-bearing term). | Use "Guilt-Free Spending" everywhere the bucket is named. | `.learn/glossary.md`; step2-retirement-design_v1.md | Content (Wave 3). Heading + comment → "Guilt-Free Spending"; subtitle reframed to Sethi's "spend on what you love." Live-verified (no "Discretionary" in DOM). **FIXED 2026-06-08.** |
| 20 | 2026-06-08 | 3 (canonical drift — tokens) | `Step5_Summary.tsx` (`#22c55e` ×6, `#f97316` ×2); violet `#8b5cf6` across Welcome/Step1/Step2/Step5; `#a78bfa`/`#34d399`/etc. | Off-canonical hexes: `#22c55e`/`#f97316` substituted for the §6 accents on glass cards; the violet "retirement/future" hue and lighter accent tints were used app-wide but **absent from canonical**. | `#22c55e`→`#10b981`, `#f97316`→`#f59e0b`. Violet ratified into §6 (`#8b5cf6` large/chart-only at 4.22:1; `#a78bfa` 6.56:1 for body text). Lighter tints permitted only as text on same-hue tinted panels. | canonical §6/§7 (ratified Wave 3) | Designer (Wave 3). Step 2 value text `#8b5cf6`→`#a78bfa` (3.45→6.56:1, live-verified); Step 5 status hexes → canonical (surplus/success now `#10b981`, live-verified). Guarded by `design-system.test.ts` row 20 (`#22c55e`/`#f97316` banned). **FIXED 2026-06-08.** |
| 21 | 2026-06-08 | 2 (false-rigor — uncaveated optimism) | `Step5_Summary.tsx` "Retirement Income Sources" table | Surviving `step5-summary_v1.md` Critical (row-9 presentation): the green "Portfolio Offset" inflated SS + pension + other at full CPI with **no adjacent caveat**, and after the §2 ratification it also contradicted the engine's fixed-nominal pension that drives the success rate on the same page. | Caveat at the table: SS keeps pace with inflation; pension/other shown at retirement-year value but not inflation-adjusted after (most pensions have no COLA), so real value erodes; the success rate reflects it; figures pre-tax. | canonical §2; step5-summary_v2.md | Content + CFP (Wave 3). Plain-language caveat added beneath the table, consistent with §2. Live-verified in DOM. **FIXED 2026-06-08.** |
| 22 | 2026-06-08 | 1 (multi-engine divergence — component recompute) | `Step3_YourNumber.tsx` (`monthlyNeedToday`, own `hasRetirementData`); `Step5_Summary.tsx` (`retMonthlySpend`) | Step 3 and Step 5 re-summed the 11 retirement sliders locally instead of reading `res.annualRetSpend`; Step 3 used its own readiness predicate instead of `res.planReady` (which Step 4/5 already read). Same value today, but the exact anti-pattern `planReady` was added to prevent. | Read `res.annualRetSpend / 12` and `res.planReady` from the single engine. | canonical §0; step3-your-number_v1.md / step5-summary_v2.md | Engineer (Wave 3). Both screens now read the engine. Live-verified (Step 3 hero + math chain render via `res.planReady`). Engine unchanged → 41 prior tests intact. **FIXED 2026-06-08.** |
| 23 | 2026-06-08 | 2 (trust / clarity) | `Step4_InvestmentPath.tsx` On-Track panel vs success banner | Two success definitions on one screen could contradict: "On Track" keys off `medianGap` (accumulation vs 25×) while the banner keys off `successProbability` (full-lifecycle drawdown survival) — "On Track" could sit beside a sub-50% success rate with no reconciliation. | When the median clears the target but success < 80%, name the gap (sequence-of-returns risk) so the two read as one story; align the banner copy regardless of gap sign. | step4-investment-path_v1.md | Content + Engineer (Wave 3). Added a `successPct < 80` caveat to the On-Track panel; fixed banner "Workable" copy. Live-verified at 89% (clean On-Track, caveat correctly absent). **FIXED 2026-06-08.** |
| 24 | 2026-06-08 | 1 (divergence) + 2 (false-rigor) | `bonus/CompoundCalculator.tsx` | Standalone tool compounded at nominal `rate/12` (so "7%" was really 7.23% effective) vs the canonical engine's effective-monthly `(1+r)^(1/12)−1`, and shipped a projection with **no estimate disclaimer** (§8); result figures were not monospace; Rule of 72 showed "Infinity years" at rate 0. | Use effective-monthly so "7%" is a true 7% annual; add an estimate disclaimer that points to Step 4 as the authoritative projection; monospace figures; guard rate 0. | canonical §3/§8; bonus-compound_v1.md | Engineer + Content (Wave 3). All four fixed; live-verified (disclaimer present, figures monospace, no Infinity). **FIXED 2026-06-08.** |
| 25 | 2026-06-08 | 2 (education gap — the "why") | `Step1_CurrentReality.tsx` (32/35 inputs bare); `Step2_RetirementDesign.tsx` (sliders) | The Wave-3 content gap: a first-timer met a wall of bare money fields — no "why," and an annual-vs-monthly foot-gun (property tax, insurance). Step 2 sliders taught the "why" only in the intro. | A short, plain-language "why"/unit per ambiguous input; a per-category rationale on each retirement slider; Sethi-true, glossary-locked, preston-writing voice. | finplan-content SKILL.md; step1/step2 audits | Content (Wave 3). 14 helper lines on Step 1's foot-gun/account inputs + monthly framing; a `why` field on all 11 Step 2 categories wired to slider helperText. Live-verified. **FIXED 2026-06-08.** |
| 26 | 2026-06-08 | 2 (trust / clarity) | `Welcome.tsx:110` (hero eyebrow) | The eyebrow "Based on the Trinity Study · JL Collins · Ramit Sethi" dropped Morgan Housel (one of the three pillars) and mixed a methodology with two author names — inconsistent with the Three Pillars section below. | Name the three thinkers the tool synthesizes consistently; keep the Trinity Study attribution where the withdrawal math is (Step 3/5). | CLAUDE.md vision; welcome_v1.md | Content (Wave 3). Eyebrow → "Ramit Sethi · JL Collins · Morgan Housel". Live-verified. **FIXED 2026-06-08.** |
| 27 | 2026-06-08 | 2 (false-rigor — stale public doc) | `README.md` | The public README documented the OLD model: `retirementReturnRate = max(4%, annualReturn × 0.6)` (ratified to ×0.85 in canonical §4) and a Monte Carlo "cone (base ±2%)" (replaced by the real seeded 1,000-path MC in Wave 1). A shipped public resource whose README contradicts its own engine and methodology page is a credibility hole. | README formulas + feature copy match canonical (×0.85, real seeded MC, COLA / pre-tax / today's-$); add the open-methodology trust statement + a CI badge. | canonical §4/§10; README | Content + Engineer (Wave 4). README drawdown → ×0.85; MC descriptions → real seeded 1,000-path with success probability; required-portfolio caveats added; Trust & Methodology section + Methodology-page pointer + CI badge added. **FIXED 2026-06-08.** |
| 28 | 2026-06-08 | 1 (performance — monolithic bundle) | `NavigationTabs.tsx` (static imports of every chart screen) | Recharts (~370 kB) was bundled into one 601 kB `NavigationTabs` chunk (gzip 177 kB) and loaded up-front even for Welcome / Step 1 / Step 3, which never chart. SWEEP-PLAN Wave 4 bundle item. | Lazy-load the four chart-bearing screens so Recharts code-splits into on-demand chunks; the initial load stays light. | SWEEP-PLAN Wave 4; build output | Engineer (Wave 4). `React.lazy` + `Suspense` on Step 2 / 4 / 5 + CompoundCalculator → NavigationTabs **601 → 188 kB** (gzip 177 → 59); CartesianChart (328 kB) now loads on demand. Build-verified; lazy mount live-verified via Suspense. **FIXED 2026-06-08.** |
| 29 | 2026-06-08 | 3 (type hygiene / CI gate) | `Step2_RetirementDesign.tsx:68`; `package.json` lint script | A pre-existing `as any` on the slider `keyMap` set, and `lint` allowed warnings — no strict gate, so type-hygiene could erode silently and CI would still pass. | Type the keyMap to the literal retirement-spend keys (no `any`); make `lint` strict (`--max-warnings 0`) so CI fails on any warning; harden CI (`npm ci`, run on every push + PR). | eslint; .github/workflows/ci.yml | Engineer (Wave 4). `keyMap` typed to the 11 retirement keys (cast removed); `lint` → `--max-warnings 0`; CI now `npm ci` + lint/test/build on every push/PR with a concurrency guard. 0 warnings. **FIXED 2026-06-08.** |

---

## Open Patterns (read this at session start)

- **Pattern 1 — multi-engine divergence.** The same financial quantity must be computed in exactly one place. Before adding any calculation to a component, check whether `financialPlan.ts` already owns it. If a number appears on two screens, the two screens must read the same store value. Never recompute a projection in a component.
- **Pattern 2 — false-rigor / false-certainty labeling.** Never describe an output as more rigorous than it is. Deterministic is not "probabilistic." A point estimate is not a "confidence interval." Every projection is an estimate, never a guarantee. Check the label against what the math actually does. See `.learn/glossary.md`.
- **Pattern 3 — canonical drift.** Every formula, reference value, and design token must trace to `.learn/canonical.md`. If the number or token you reach for is not in canonical.md, do not use it — add it to canonical first (with a source), then use it.
- **Pattern 4 — accessibility regression.** Every text/background pair must clear the canonical contrast table (§7). Every input needs an associated label. Status is never conveyed by color alone. Run the a11y checklist before any component ships.
- **Pattern 5 — silent state loss / input foot-gun.** Never lose or silently overwrite user input. Progress persists across refresh. A value the user typed is not overwritten by a default without intent.

---

*Errors log v1.0 | 2026-06-06 | Append-only | Consumed by: all finplan-* skills*
