// One card, not three (designer standard / errors.md row 4). MetricCard is the metric-tile
// vocabulary for the single card component — a true alias of FintechCard, so the two can never
// drift. Both render the same glass card with the same canonical §6 variant top-border.
export { FintechCard as MetricCard } from './FintechCard';
