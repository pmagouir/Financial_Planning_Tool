# Spec: [feature / fix name]
# Owner: finplan-[agent] | Created: [date] | Status: draft → in-progress → shipped

## Problem
[What's wrong or missing. If this closes an `errors.md` row, cite it: "Closes errors.md row N."]

## The change
[Concretely, what changes. Files touched, store keys added/changed, components affected.]

## Canonical references
[Every formula, reference value, or token this touches, by its `canonical.md` section. If the change needs a value not yet in canonical, add it to canonical FIRST (with a source), then list it here. A spec that introduces an untraced number will not pass review.]

## Sources & Assumptions
[REQUIRED — the gate blocks a spec without this section.]
- Source: [primary source for any methodology or value, with confidence note]
- Assumption: [any modeling or design assumption, stated plainly]

## Acceptance criteria (what QA tests)
- [ ] [Specific, checkable outcome — e.g., "Step 4 cone median equals store `projectedPortfolio` to the cent for the reference inputs"]
- [ ] [Reference-value assertion against canonical §5]
- [ ] [Regression test added for the `errors.md` row this closes]

## Out of scope
[What this spec deliberately does not do, and which agent owns it instead.]
